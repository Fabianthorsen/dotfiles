- clean: ["~"]

- link:
    ~/.config/nvim:
        path: config/nvim
    ~/.zshrc: 
        path: zsh/zshrc
        force: true
    ~/.oh-my-zsh: zsh/oh-my-zsh
    ~/.tmux.conf: tmux.conf

- shell:
    - [git submodule update --init --recursive, Installing submodules]
