- clean: ["~"]

- link:
    ~/.config/nvim:
        path: config/nvim
    ~/.zshrc: zshrc
    ~/.tmux.conf: tmux.conf
    ~/.tmux:
        path: tmux

- shell:
    - [git submodule update --init --recursive, Installing submodules]
